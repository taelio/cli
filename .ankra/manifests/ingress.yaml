apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tael-cli
  labels:
    app.kubernetes.io/name: tael-cli
  annotations:
    ankra.io/generated: "true"
    cert-manager.io/cluster-issuer: ${{ ankra.ingress_tls_issuer }}
spec:
  ingressClassName: ${{ ankra.ingress_class }}
  tls:
    - hosts:
        - ${{ ankra.ingress_host }}
      secretName: tael-cli-tls
  rules:
    - host: ${{ ankra.ingress_host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tael-cli
                port:
                  number: 80
